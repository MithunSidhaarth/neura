[build]
  base = "."
  command = "corepack enable && cd apps/client && yarn install && yarn build"
  publish = "apps/client/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"